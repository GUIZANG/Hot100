# 第 0 关 · C++ 地基检测

## 检测 0A：数组输入输出

### 输入

```text
5
3 1 4 1 5
```

第一行是数组长度 `n`，第二行是 `n` 个整数。

### 预期输出

```text
5 1 4 1 3
```

### 要求

1. 使用 `cin` 读取 `n`。
2. 创建长度为 `n` 的 `vector<int>`。
3. 使用循环读取所有元素。
4. 使用循环倒序输出，元素之间用空格分隔。
5. 暂时不要使用 `reverse`。
6. 在 `00-C++地基答题.cpp` 中闭卷完成。

完成后告诉 AI：“检测 0A 写完了”。

### 检测结果

已通过：能够使用 `cin`、`vector` 和循环完成数组读写。

---

## 检测 0B：LeetCode 函数模式

实现函数 `doubleValues`：

- 输入：`vector<int>& nums`
- 输出：一个新的 `vector<int>`
- 新数组中的每个元素等于原数组对应元素的两倍

示例：

```text
输入：nums = [1, -2, 3]
输出：[2, -4, 6]
```

### 要求

1. 使用 LeetCode 的 `class Solution` 格式。
2. 不写 `main`、`cin` 和 `cout`。
3. 不修改原数组。
4. 使用 `push_back` 构造并返回新数组。
5. 在 `00B-LeetCode函数检测.cpp` 中闭卷完成。

完成后保存文件并告诉 AI：“检测 0B 写完了”。

### 检测结果

提示后通过：已经理解参数是输入、局部 `vector` 用于收集答案、`return` 返回结果。

---

## 检测 0C：通过引用原地修改

实现函数 `increment`：

- 输入：`vector<int>& nums`
- 输出：无返回值，返回类型为 `void`
- 操作：把 `nums` 中的每个元素原地增加 `1`

示例：

```text
调用前：nums = [1, -2, 3]
调用后：nums = [2, -1, 4]
```

### 要求

1. 使用 LeetCode 的 `class Solution` 格式。
2. 不写 `main`、`cin`、`cout`，也不创建结果数组。
3. 通过引用参数直接修改 `nums`。
4. 在 `00C-vector引用检测.cpp` 中独立写出完整函数。

完成后保存并告诉 AI：“检测 0C 写完了”。

### 检测结果

修正函数名后通过：能够通过引用原地修改 `vector`。

---

## 检测 0D：独立迁移检测

实现函数 `positiveOnly`：

- 输入：`vector<int>& nums`
- 输出：一个新的 `vector<int>`
- 新数组只保留原数组中大于 `0` 的元素，并保持原顺序

示例：

```text
输入：nums = [-2, 3, 0, 5, -1]
输出：[3, 5]
```

### 要求

1. 不使用 `cin`、`cout`。
2. 不修改原数组。
3. 独立写出完整函数，不提供填空提示。
4. 在 `00D-vector迁移检测.cpp` 中完成。

完成后保存并告诉 AI：“检测 0D 写完了”。

### 检测结果

独立通过：输入 `[-2, 3, 0, 5, -1]`，程序正确输出 `[3, 5]`。

第 0 关已通关。
